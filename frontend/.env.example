# Base URL for the BizPulse Django REST API
# In local development with the backend running, use http://localhost:8000
# If running purely offline/demo without a backend, this can remain default
NEXT_PUBLIC_API_URL=http://localhost:8000
